[package]
name = "eurowl"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroWL — de ECHTE Wayland-wire-protocol-server-kern voor EuroDisplay (plan H5). Pure no_std-logica, host-getest."

[dependencies]
