[package]
name = "eurohealth"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroHealth — SMART-parsing + systeem-gezondheidsscore (plan Z). no_std, host-testbaar."

[dependencies]

[features]
default = []
