[package]
name = "euroupdate"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroUpdate — atomische A/B-systeemslots met rollback (Track 9 / plan F1). Pure no_std-logica, host-getest."

[dependencies]
