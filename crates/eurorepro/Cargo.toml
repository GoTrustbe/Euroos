[package]
name = "eurorepro"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroRepro — reproduceerbare builds (plan M3/Q2): deterministische build-spec-hash, getekende build-attestaties, onafhankelijke-reproductie-consensus. no_std, host-testbaar."

[dependencies]
ed25519-dalek = { version = "2", default-features = false }
sha2 = { version = "0.10", default-features = false }

[features]
default = []
