[package]
name = "euroclock"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroClock — de klok-app van EuroOS (AC-2). Wereldtijd (per tijdzone, 12/24u-notatie), wekker, timer en stopwatch — pure tijdslogica. no_std, host-testbaar."

[dependencies]

[features]
default = []
