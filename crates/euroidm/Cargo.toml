[package]
name = "euroidm"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroIDM — enterprise identity (plan V): gebruikers/groepen, groep→capability-mapping, getekende OIDC-achtige tokens. no_std, host-testbaar."

[dependencies]
ed25519-dalek = { version = "2", default-features = false }

[features]
default = []
