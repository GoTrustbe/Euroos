[package]
name = "euromail"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroMail — de e-mailkern van EuroOS (AC-4). RFC822/MIME-berichtparser: headers (unfold), adreslijsten, multipart-MIME, base64 + quoted-printable decodering, en RFC2047 encoded-words. De IMAP/SMTP-transportlaag komt later op euronet/eurotls. no_std, host-testbaar."

[dependencies]

[features]
default = []
