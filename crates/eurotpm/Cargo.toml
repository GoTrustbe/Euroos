[package]
name = "eurotpm"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroTPM — TPM 2.0 commando-codering (plan O1, hardware root of trust). no_std + alloc, host-testbaar."

[dependencies]

[features]
default = []
