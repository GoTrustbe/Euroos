[package]
name = "eurofw"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroFW — stateful packet-filter / firewall (plan N3). no_std, host-testbaar."

[dependencies]

[features]
default = []
