[package]
name = "eurontfs"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroNTFS — a from-scratch read-only NTFS driver ($MFT, USA fixup, attributes, runlists, resident + non-resident data). Verified vs mkntfs. no_std, host-tested."

[dependencies]

[features]
default = []
