[package]
name = "euroshot"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroShot — de schermafbeelding-tool van EuroOS (AC-1). Regio-/venster-/volledig-scherm-geometrie, annotaties (pijl/kader/markering/tekst), en een canoniek gesigneerd manifest (Ed25519-payload: 'dit screenshot is ongewijzigd, gemaakt op <ts>'). no_std, host-testbaar."

[dependencies]

[features]
default = []
