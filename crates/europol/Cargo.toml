[package]
name = "europol"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroPol — declaratieve policy-engine die menselijk beleid naar EuroGuard-capabilities vertaalt (plan X). no_std, host-testbaar."

[dependencies]
ed25519-dalek = { version = "2", default-features = false }

[features]
default = []
