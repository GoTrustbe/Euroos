[package]
name = "eurocrash"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroCrash — kernel crash-dump-formaat + recovery (plan Y). no_std, host-testbaar."

[dependencies]

[features]
default = []
