[package]
name = "euroacpi"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroACPI — ACPI-tabel-parsing (SDT-headers, checksums, RSDT/XSDT, FADT power-velden) kern (plan I3). Pure no_std-logica, host-getest."

[dependencies]
