[package]
name = "eurogpu"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroGPU — virtio-gpu commandoprotocol (plan K4): 2D-resource-aanmaak, scanout, transfer, flush — serialisatie + response-parsing. no_std, host-testbaar."

[dependencies]

[features]
default = []
