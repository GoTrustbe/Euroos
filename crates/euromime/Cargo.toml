[package]
name = "euromime"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroMime — MIME-type detection (extension + magic bytes) + default-app associations + open-with resolution (3F-5). no_std, host-testbaar."

[dependencies]

[features]
default = []
