[package]
name = "eurocalc"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroCalc — formule-engine voor EuroSuite Calc (ES-Calc): formule-parser + evaluator over het EuroDoc-rekenblad, met celverwijzingen, bereiken, functies en cyclusdetectie. no_std, host-testbaar."

[dependencies]
eurodoc = { path = "../eurodoc" }

[features]
default = []
