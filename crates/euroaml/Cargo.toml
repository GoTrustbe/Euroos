[package]
name = "euroaml"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroAML — minimale ACPI-AML-bytecode-interpreter (plan I3, power management). no_std + alloc, host-testbaar."

[dependencies]

[features]
default = []
