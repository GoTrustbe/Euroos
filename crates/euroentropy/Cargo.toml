[package]
name = "euroentropy"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroEntropy — early-boot CSPRNG: HMAC-DRBG (NIST SP 800-90A, KAT-verified) seeded from jitter + TPM, with a getrandom readiness gate. no_std."

[dependencies]
sha2 = { version = "0.10", default-features = false }

[features]
default = []
