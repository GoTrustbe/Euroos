[package]
name = "eurowasm"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroWASM — een minimale no-JIT WASM-interpreter + WASI-op-EuroGuard-capabilities (plan H4). Pure no_std-logica, host-getest."

[dependencies]
