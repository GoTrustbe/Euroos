[package]
name = "eurofsid"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroFSID — filesystem identification from a superblock (btrfs/xfs/ntfs/exfat/fat32/ext), for mount auto-detect. no_std, host-tested vs real mkfs images."

[dependencies]

[features]
default = []
