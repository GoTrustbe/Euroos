[package]
name = "euroreken"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroReken — de rekenmachine van EuroOS (AC-1). Expressie-evaluator met standaard-, wetenschappelijke- en programmeursmodus (hex/oct/bin + bitwise), plus eenhedenconversie. Soevereine no_std-mathkern (sqrt/exp/ln/sin/cos/pow zonder libm). Host-testbaar."

[dependencies]

[features]
default = []
