[package]
name = "eurofiles"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroFiles — de bestandsbeheerder van EuroOS (AC-1). Map-model met sorteren/filteren, padbewerkingen (normaliseren/join/basename), mensvriendelijke groottes en soevereine badges (immutable/getekend/append-only/versleuteld). no_std, host-testbaar."

[dependencies]

[features]
default = []
