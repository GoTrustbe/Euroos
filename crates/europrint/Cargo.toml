[package]
name = "europrint"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroPrint — IPP (Internet Printing Protocol) request-building + response-parsing (plan I4). Pure no_std-logica, host-getest."

[dependencies]
