[package]
name = "euromdns"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroMDNS — multicast DNS (RFC 6762) + DNS-SD service discovery: .local name resolution, A/AAAA/PTR/SRV/TXT responder. no_std, host-tested."

[dependencies]
eurodns = { path = "../eurodns" }

[features]
default = []
