[package]
name = "eurodocio"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroDocIO — document-I/O voor EuroSuite (ES-IO): eigen XML-parser + OOXML/ODF lezen+schrijven + HTML-export, naar/uit het EuroDoc-UDM. no_std, host-testbaar."

[dependencies]
eurodoc = { path = "../eurodoc" }

[features]
default = []
