[package]
name = "euroclip"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroClip — de klembordbeheerder van EuroOS (AC-2). Geschiedenis met vastmaken, dedup, GDPR-vriendelijke auto-expiratie (geschiedenis enkel naar schijf indien vastgemaakt) en automatische uitsluiting van wachtwoord-achtige inhoud. no_std, host-testbaar."

[dependencies]

[features]
default = []
