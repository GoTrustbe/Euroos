[package]
name = "eurodisplay"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroDisplay — Wayland-vormig display-protocol + compositor-surface-model (plan E2). Pure no_std-logica, host-getest."

[dependencies]
