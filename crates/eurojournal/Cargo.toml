[package]
name = "eurojournal"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroJournal — a structured, severity-tagged, queryable system journal (journald-equivalent) with JSON export and a bounded ring. no_std, host-tested."

[dependencies]

[features]
default = []
