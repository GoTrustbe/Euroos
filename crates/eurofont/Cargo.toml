[package]
name = "eurofont"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroFont — het lettertypebeheer van EuroOS (AC-2). Parseert sfnt/TrueType-fonts (tabel-directory + name/head/maxp): familienaam, stijl, units-per-em, aantal glyphs. Bevat een minimale font-bouwer voor tests. no_std, host-testbaar."

[dependencies]

[features]
default = []
