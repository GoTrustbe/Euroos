[package]
name = "europkg"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroPkg — pakketbeheerder (plan M2): semver-versies + constraints, dependency-resolutie (topologisch, conflict/cyclus-detectie), getekende manifesten. no_std, host-testbaar."

[dependencies]

[features]
default = []
