[package]
name = "eurosandbox"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroSandbox — capability-scoped containers via het EuroGuard-model (plan F2). Pure no_std-logica, host-getest."

[dependencies]

[dev-dependencies]
ed25519-dalek = { version = "2", default-features = false, features = ["rand_core"] }
