[package]
name = "euroid"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroID — soeverein gebruikersbeheer (Sprint K1): UserId/GroupId, Argon2id-credentialopslag, sessies, per-gebruiker EuroGuard-caps, wachtwoordbeleid, en een hash-chain audit-log. no_std, host-testbaar."

[dependencies]
sha2 = { version = "0.10", default-features = false }

[features]
default = []
