[package]
name = "euroinstall"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroInstall — guided installer/live-image planner (plan Q1): partitielayout, validatie, stappenplan. no_std, host-testbaar."

[dependencies]
eurolocale = { path = "../eurolocale" }

[features]
default = []
