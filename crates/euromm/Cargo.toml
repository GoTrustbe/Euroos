[package]
name = "euromm"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroMM — fysiek geheugenbeheer (Track 3). no_std + alloc, host-testbaar."

[dependencies]
