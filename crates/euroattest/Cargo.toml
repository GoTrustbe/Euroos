[package]
name = "euroattest"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroAttest — remote attestation (plan O2): TPM-quote over PCR-set + nonce, challenge/response, verificatie. no_std, host-testbaar."

[dependencies]
ed25519-dalek = { version = "2", default-features = false }

[features]
default = []
