[package]
name = "eurogeo"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EuroGeo — IP-to-country attribution (longest-prefix CIDR match) for EuroGuard geo-blocking. Curated major allocations, extensible from a data feed. no_std, host-tested."

[dependencies]
