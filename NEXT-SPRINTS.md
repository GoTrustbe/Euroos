# EuroOS — Phase 2 Sprint Plan (what's still open)

> **Status 2026-06-14:** the **forward plan** ([`docs/SPRINT-PLAN-FORWARD.md`](docs/SPRINT-PLAN-FORWARD.md), Sprints 1–5) is ✅ done — TLS dynamic linking, clean ACPI shutdown, **secure A/B self-update** (loader-owned attempt-counter + auto-rollback + **Ed25519 verify-before-activate**), new desktop apps (EuroText/Monitor/Log), web `POST` + in-page JS, and reliability hardening (crash-consistency fault-injection, lock-order checker, block-under-load). Then a **storage-interoperability sprint** ([`docs/SPRINT-PLAN-INTEROP.md`](docs/SPRINT-PLAN-INTEROP.md), IO-1…7 ✅): mountable **FAT32 (r/w), exFAT (r), ext2/3/4 (r), SMB2/3 + NTLMv2, NFSv3**, a `format`/mkfs command and an auto-detecting `mount`/`lsblk` — each verified against the real reference tools — plus **multi-disk load+functional testing 8 MiB → 64 GiB**. The whole codebase was also **translated to English**. **755 host tests.** Next candidates: exFAT/ext write, btrfs/xfs/NTFS read, the real partition/USB-disk mount harness, and the hardware-attended items (WiFi/printer/S3).
>
> *(Earlier: the AD–AG "Promise = Reality" cycle ✅ — Zero-Trust depth, EuroID end-to-end, desktop apps, web images+forms, the sovereign `eurofat` installer, coreutils `xargs`; AH-1/2/3 ✅. `SPRINT-PLAN-AD-AG.md` + `SPRINT-PLAN-AH.md` are those completed cycles. The G–Q board below is the underlying item tail.)*

*Successor to the A–F plan (now mostly ✅/🟡). Consolidates every open item from
[IMPLEMENTATION-PLAN.md](IMPLEMENTATION-PLAN.md) (the 🟡 remainders) **and** the
untouched subsystems in `docs/planning/` (Missing_Subsystems #1–16, Track8 EuroCompat,
Track9 EuroUpdate). Generated 2026-06-04 · expanded with sprints L–Q 2026-06-05 · 181 host tests green.*

Two kinds of open work:
- **(R)** finish a started core — the 🟡 remainders; the host-tested core + kernel hooks already exist.
- **(N)** a new/untouched subsystem from the roadmap.

Pick by **value × safety**. 🧪 host-testable · 🔒 boot-risky/attended · 🏗️ large multi-component.

```
SPRINT G   SPRINT H   SPRINT I   SPRINT J   SPRINT K   SPRINT L     SPRINT M
Finish     App eco-   Hardware   Concur-    Sovereign  Immutab-     Toolchain
the cores  system     breadth    rency      polish     ility        & dev-ex
SPRINT N   SPRINT O   SPRINT P   SPRINT Q
Network    Sovereign  EU compl-  Distrib. &
complete   id & trust iance/l10n governance
```

---

## Sprint G — Finish the foundations (close the 🟡 cores) — **highest value/effort ratio**
| ID | Task | Kind | Notes |
|----|------|------|-------|
| G1 | **A2** — migrate *all* kernel stacks to guarded stacks | ✅ (R 🔒) | DONE: generalized A2 into a **multi-stack guarded allocator** (`paging::guarded_stack_alloc`, uniform 5-page units = 1 unmapped guard + 4×4 KiB stack, O(1) region+modulo `is_stack_guard`). Added a **dedicated page-fault IST** (`gdt::PAGE_FAULT_IST_INDEX`) so an overflow's exception-push lands on a fresh stack (no double-fault). Migrated the **7 AP stacks** *and* the **scheduler per-task stacks** (`sched::set_task_guarded_stack`, BSS fallback). **Recoverable deliberate-overflow self-test** (task 5 recurses into its guard → PF handler kills only that task via `mark_current_dead`+reschedule). **Verified `-smp 4`**: overflow caught at guard `0x8000019f70`, killed, 3/3 APs online + per-CPU schedulers, S2 prio tasks ran on guarded stacks, 3-window desktop up, 0 faults beyond the intentional one. `run-multidisk.py` takes `SMP=` env. *(Optional remainder: IST `DF_STACK`/`RSP0_STACK` — pre-paging, needs TSS reload, attended.)* |
| G2 | **B3+B2** — VFS path-routing + EuroFS-on-NVMe | R 🧪 | Mount table so the shell routes `/mnt/*` by longest-prefix (`EXDEV` cross-mount); all-partition GPT parse + `/etc/fstab` + `mount`/`umount`/`df` commands; wrap NVMe as a `BlockDevice` and mount EuroFS on it. |
| G3 | **C2+C1** — select/poll + reliable server | ✅ core (R 🧪) | DONE: `net::sock_poll(fds, deadline)` — non-blocking readiness multiplexing over a fd-set (Listen readable ⇔ accept-queue non-empty; Conn readable ⇔ rx data or EOF), drives `service()`+`pump` once per spin, tick-deadline + 2M-spin safety cap. Boot self-test `[g3]` multiplexes a fresh TCP listener + UDP socket and proves the listener is *not* readable (empty queue). DEFERRED (no userspace consumer to verify against): Linux `poll(2)` syscall plumbing over user pollfd array; rewriting `:80` as an unmodified C binary; keepalive/cwnd-pacing; Nagle/`TCP_NODELAY`. |
| G4 | **F1** — real atomic A/B updates | 🟡 (R 🔒) | DONE: (a) raw-block `slot_config` (below); (b) **multi-partition A/B GPT** — `install` writes EuroOS-A/B + EuroVar + EuroBoot, `/var` mounts the EuroVar partition (root-mount path unchanged), verified `[g4]`; (c) **image→slot-partition write** — `apply` writes the verified image to the inactive slot's partition (sector I/O + read-back), verified `[g4] slot-image-write … ✓`. (d) **two-stage `loader.efi`** ✅ — a separate UEFI binary (`loader/` crate) is now `BOOTX64.EFI`; reads `slot_config`, picks the slot, `LoadImage`/`StartImage`s `eurokernel-{A,B}.efi`. Verified: `[loader] boot slot A → LoadImage+StartImage → kernel bring-up → desktop`, 0 faults. *(refinement: unify the loader's slot source with the kernel's raw-block LBA-40 config via BlockIO + run the rollback counter in the loader.)* — A/B `slot_config` moved off an EuroFS *file* onto a **raw GPT-reserved block** (LBA 40, in the alignment gap before the first partition at LBA 2048 — outside any EuroFS). Source-of-truth = raw block (FS file kept as a readable mirror); read/written via direct `virtio_blk` sector I/O + flush. Survives FS corruption/torn-writes (the top reliability risk). **Verified across a real reboot**: boot 1 "verse schijf → initieel", boot 2 "HERSTELD van vorige boot" (`[g4]`, persistent disk). The anti-brick state machine now runs on durable raw storage. DEFERRED (the two-stage architecture, multi-sprint): multi-partition GPT (slot A/B + /var + /boot), image writes to partition block-ranges (currently still a `/boot/slot_X.img` file), and a separate **loader.efi** that reads `slot_config` and loads the chosen slot's kernel (today the kernel *is* the single-stage UEFI app, so it runs the slot decision itself). |
| G5 | **B1** — background data scrubber | ✅ (R 🧪) | DONE: `scrub::run`/`maybe_run` — a paced EuroFS integrity pass (superblock + structure + per-inode data-path XXH3) logged to `/var/log/fsck.log` (the real EuroVar partition, G4). Runs once at boot + periodically (rate-limited ~60 s) from the desktop tick. Verified `[g5] scrub #1: 54 inodes, 183 blocks, data-verified 52, errors 0, superblock OK, bitmap OK`, 0 faults. *(FS owned by the desktop loop → tick-driven rather than a separate nice+19 task; an incremental chunked-crawl scrub API is a future refinement.)* |

## Sprint H — App ecosystem (EuroCompat layers 2–4 + display) — **unlocks real software**
| ID | Task | Kind | Notes |
|----|------|------|-------|
| H1 | **Unix-domain sockets** (`AF_UNIX`) | ✅ (N 🧪) | DONE: `euronet::unix::Switchboard` — one kernel-wide local-socket switchboard (owns all listeners + connections, no shared ownership). `bind_listen`/`connect`/`accept`/`send`/`recv`/`readable`/`close` with POSIX-shaped errors (ECONNREFUSED/EADDRINUSE/backlog/EPIPE/EOF), bidirectional byte FIFOs, lightweight copyable `Endpoint` handle. Kernel API `net::unix_*` + boot self-test `[h1]` proves a full local round-trip (ping/pong + EOF-after-close). **8 host tests.** This is the prerequisite H2 consumes (compositor ↔ app over local sockets). *(deferred: AF_UNIX `socket()` syscall over the Linux ABI + fd-passing — wired when a userspace consumer exists; SCM_RIGHTS later.)* |
| H2 | **E2** — live display server | ✅ (R 🏗️) | DONE: `eurodisplay::server` — a length-prefixed frame protocol carrying `Request`s + compositor metadata (Title/Line) over a byte stream, and `ServerView` translating mapped surfaces → `WindowView` (geometry + title + content). Kernel `dispserv::DispServer` binds AF_UNIX `/run/eurodisplay.sock`, accepts clients, decodes frames, and emits real `compositor::Window`s drawn by EuroDesktop. An in-kernel demo app connects over AF_UNIX (H1) and opens a live 3rd window — verified `[h2]` + `compositor actief — 3 vensters`, 0 faults. **7 host tests.** *(deferred: input Events back to the app, per-tick live updates from a real userspace app, pixel buffers/SHM.)* |
| H3 | **E1** — ld.so dynamic linker | 🟡 core (R 🏗️) | DONE (in-kernel linker): `ring3::dynlink_selftest` + `find_export`/`link_symbol_relocations`/`dyn_value` — loads a DT_NEEDED `.so` into the process arena at a sub-offset (W^X bitmap merged, shifted), reads its dynamic symbol table (GNU_HASH-robust symbol count via `(STRTAB−SYMTAB)/SYMENT`), and patches the executable's **R_X86_64_JUMP_SLOT + GLOB_DAT** GOT slots with resolved addresses. Verified `[h3]`: `dyntest.elf` (dynamically linked, PIE) calls `euro_answer()` from `libeuro.so` → `output="H3: 42"` exit 42, 0 faults. New userland: `dynlib.c`→`libeuro.so`, `dyntest.c`→`dyntest.elf`. **+ run-by-name from the FS** (`ring3::needed_libs` parses DT_NEEDED → resolves `.so`s from `/lib`; `run_dynamic` generalized to N libs): `[h3-fs] /bin/dyntest deps=["libeuro.so"] → 1 .so uit /lib → exit 42`. FOLLOW-ON (large): busybox/curl/sqlite breadth against a real `libc.so` (many more syscalls + symbol versioning/TLS/IRELATIVE relocs). |
| H4 | **F2** — WASM/WASI runtime | 🟡 core (R 🏗️) | DONE: **`eurowasm` crate** — a no-JIT WASM interpreter (LEB128 + section parser; stack machine: i32/i64 arithmetic+compares, locals, `block`/`loop`/`if`/`else`/`br`/`br_if`/`return`, `call`+recursion, linear memory `i32.load`/`store`/`memory.grow`, `drop`/`select`/globals). **WASI→EuroGuard capabilities** via a `HostImports` trait (`fd_write` cap-gated). **7 host tests** (sum-loop=5050, fac(5)=120, capability-gated fd_write). Kernel `wasm::selftest` `[h4]`: `run()=55` + `fd_write` writes from WASM memory — allowed with CAP_CONSOLE, **denied without** — 0 faults. **+ process→container binding** (`wasm::container_selftest`, `[h4-ctr]`): the SAME module's WASI `sock_connect` runs under three **EuroSandbox containers** — *allowed* (CAP_NET + net-scope OK), *denied* (CAP_NET revoked), *denied* (net-scope forbids the host) — WASI governed by the real `Container::effective_caps`/`allow_connect`. **+ f32/f64 floating-point** (convert/arith/compare/trunc, `no_std`-pure — abs/neg via bit-ops, no libm; host-tested `fcomp(n)=trunc(n*2.5+0.5)`). FOLLOW-ON: full WASI preview1 (real `fd_read`/`path_open`/`sock_*`); a `wasm <file>` shell command. |
| H5 | **X11/Wayland bridge** (EuroXServer) + **Flatpak runtime** | 🟡 core (N 🏗️) | DONE: **`eurowl` crate** — the REAL Wayland **wire protocol** (vs H2's Wayland-*shaped* one): 8-byte headers `[obj_id][(size<<16)\|opcode]` + word-aligned args, and a minimal compositor server (`wl_display`/`wl_registry`/`wl_compositor`/`wl_surface` + `xdg_wm_base`/`xdg_surface`/`xdg_toplevel`). A full handshake (`get_registry`→`bind`→`create_surface`→`get_xdg_surface`→`get_toplevel`→`set_title`→`commit`) produces a titled window. **7 host tests.** Kernel `wayland::selftest` `[h5]` runs the real handshake in-kernel → renders a **4th desktop window** (`compositor actief — 4 vensters`), 0 faults. FOLLOW-ON (large): run *unmodified* libwayland clients over AF_UNIX (H1) — fd-passing/SHM buffers, the wl_shm pool, damage/frame callbacks; an X11 server; the Flatpak-style sandboxed runtime. |

## Sprint I — Hardware breadth (untouched device subsystems) — **bare-metal readiness**
| ID | Task | Kind | Notes |
|----|------|------|-------|
| I1 | **USB stack** | ✅ **DONE** (HID) | Protocol core: **`eurousb` crate** — USB descriptor parsing + **HID boot protocol** (`BootKeyboard`, `parse_mouse`). **6 host tests.** **+ full `xhci` controller driver wired** (`kernel/src/xhci.rs`): MMIO BAR bring-up, controller reset, DCBAA + scratchpad buffers, **command ring + event ring** (TRB rings w/ Link+cycle), port reset, and the real USB enumeration chain — **Enable Slot → Address Device → GET_DESCRIPTOR(device+config) → SET_CONFIGURATION → Configure Endpoint → SET_PROTOCOL(boot)** — then live **interrupt-IN polling** that feeds reports into the existing PS/2 scancode + mouse paths. **Boot-verified** on `qemu-xhci` + `usb-kbd`/`usb-mouse`: both devices enumerate (`0627:0001`), and QMP-injected keystrokes/mouse-moves arrive as correct interrupt reports (`[xhci-rpt] kbd: ..08.. = 'e'`, `muis: 14 0a = dx20/dy10`). **+ USB mass storage** (`eurousb::bot` host-tested CBW/CSW/SCSI builders + xHCI **bulk-endpoint transport** + Bulk-Only-Transport): Configure-Endpoint for bulk IN+OUT, then **SCSI INQUIRY → READ CAPACITY → READ(10)**. **Boot-verified** on `usb-storage`: identifies "QEMU QEMU HARDDISK", reads capacity (16 MiB), and READ(10) sector 0 returns the exact bytes written to the image (`"EUROUSBD"`). `usb_read_block` API exposed. **9 host tests** (eurousb). REMAINING: USB hubs (multi-tier); writeback + mounting a USB EuroFS volume. |
| I2 | **Audio subsystem** | ✅ **DONE** | Mixer core: **`euroaudio` crate** — `no_std` software mixer (multi-stream i16 PCM + Q8 volume + clip-protection) + format conversions + resampling. **9 host tests.** **+ full `hda` Intel HD-Audio driver wired** (`kernel/src/hda.rs`): MMIO bring-up, controller reset, **CORB/RIRB** command rings, **codec enumeration** (root → audio-function-group → DAC + output-pin), output-path routing (converter format/stream, amp gains, pin output + EAPD), and a **stream-DMA** with a 2-entry BDL playing a `euroaudio::mix`-generated two-tone signal. **Boot-verified** on `intel-hda` + `hda-output`: codec responds (`vendor 0x1af40012`), DAC=node 2/pin=node 3 routed, stream runs with **LPIB advancing 0→204 = DMA consuming the buffer (audio playing)**. (Fix: raise RINTCNT + clear RIRBSTS each verb, else QEMU stalls CORB DMA after 1 response.) REMAINING: capture/input streams; AC'97 fallback; per-app volume routing. |
| I3 | **ACPI power management** | 🟡 started (N 🔒) | DONE (parser core): **`euroacpi` crate** — SDT-header parsing + **checksum validation**, RSDT (32-bit) / XSDT (64-bit) table-pointer enumeration, and **FADT (FACP)** power fields (`PM1a_CNT_BLK` for S5 shutdown, `RESET_REG`/`RESET_VALUE` for reboot). **6 host tests.** (The kernel already does S5 shutdown + reboot via these registers; this is the host-tested, reusable parser.) **+ full AML interpreter** (`euroaml` crate): a minimal ACPI-AML bytecode interpreter — namespace build (`Scope`/`Name`/`Method` + ext-ops `Device`/`ThermalZone`/`PowerResource`/`Processor` recursion, `OperationRegion`/`Field`/`Mutex` skipping), integer/buffer/string/package data objects, PkgLength decoding, and method evaluation (`Return` of constants/packages/arithmetic). **8 host tests** + an encoder. **Boot-verified on QEMU's real DSDT** (`[i3-aml]`: interprets the 8.6 KB DSDT → 39 objects, evaluates `\_S5` = `[0,0,0,0]`, finds `_STA`/`_S5`). REMAINING: `_TMP`/`_BST` need a QEMU thermal-zone/battery; OperationRegion/Field *side-effect* evaluation (EC reads); S3 suspend/resume (not headless-verifiable). |
| I4 | **Printer & scanner** | 🟡 started (N 🏗️) | DONE (IPP core): **`europrint` crate** — IPP (RFC 8010/8011) request building (`Print-Job`/`Get-Printer-Attributes`, charset/language/printer-uri/job-name/keyword/integer attrs, document append) + response parsing (status + attributes). **5 host tests.** **+ HTTP transport** (`europrint::http`): builds the IPP-over-HTTP `POST` envelope (`Content-Type: application/ipp` + `Content-Length`) and parses the HTTP response (status + `Content-Length`/`chunked` body decode) — the bytes ride EuroNet (port 631) / EuroTLS (ipps). **9 host tests.** REMAINING: wire the transport to a live `euronet` TCP connect (verify against a CUPS/IPP server); SANE-style scanning. |

## Sprint J — Concurrency, scale & reliability
| ID | Task | Kind | Notes |
|----|------|------|-------|
| J1 | **D1b/c** — per-subsystem locking | 🟡 started (R 🔒) | **+ lock-free kmsg DONE** (`kernel/src/klog.rs`): the global `Mutex` that was taken on *every* serial log line (incl. from IRQs / multiple cores → contention + panic-handler deadlock risk) is replaced by a **wait-free MPSC ring** — writers claim a slot with `HEAD.fetch_add(1)` and publish the length with Release; the partial (unterminated) line lives **per-CPU** (`PCUR`/`PLEN`, gated so `lapic_id()` is only read after the APIC is mapped). Readers (dmesg/panic) are lock-free → the panic handler can never block. **Boot-verified on 4 cores**: 3 APs log concurrently via this path (`core APIC-id N online`), `[j1]` self-test claims 64 lines + reads them back intact, no faults. **+ concurrent block-cache DONE** (`eurofs::cache::BlockCache`): a write-through cache over any `BlockDevice` where a **cache hit takes only a read-lock** (`RwLock`; the CLOCK ref-bit + hit/miss counters are atomic) so multiple cores read cached blocks in parallel — only a miss/eviction/write takes the write-lock + device `Mutex`. CLOCK/second-chance eviction, dirty write-back. **6 host tests incl. an 8-thread concurrency test** (4000 parallel reads, data-consistent, no deadlock/corruption) **+ `BlockCache` itself impls `BlockDevice`** (transparent drop-in: a real `EuroFs` formats+mounts *through* the cache in a host test). **Boot-verified no_std** (`[j1-cache]`: write-through block + 2 read-lock hits + 1 miss, data-intact). REMAINING: per-mount FS / per-connection net / per-CPU run-queue / per-channel IPC spinlocks; concurrent socket table; swap the live root-FS type (`EuroFs<RootBlk>` → `EuroFs<BlockCache<RootBlk>>`, ripples through `fs`/`fs2`/`vfs`). |
| AfM | **Afmaak-sprint** (R-vervolg) | ✅ **DONE** | Closed started work + own additions. **HLT-idle** (desktop yields the CPU between frames → energy-saving; system stays live — `[init]` ticker keeps running). **IRQ-driven USB input** (xHCI harvest moved into the MSI-X handler with a re-entrancy guard, so input works under HLT-idle/preemption). **`\_S5`-driven shutdown** (the AML-evaluated `_S5` SLP_TYP feeds `power::shutdown`'s PM1a value — firmware-correct vs hardcoded 0). **E2E interactivity test** (`scripts/run-e2e.py`: types `lsdev` via the USB keyboard → full shell exec + output verified on serial — proves USB-key→xHCI-IRQ→scancode→shell→exec end-to-end). **Finished J2**: virtio-blk MSI-X (see J2 row). |
| J2 | **B2 depth** — NVMe robustness | 🟢 **MSI-X done** (R 🧪) | DONE (bad-block remap): **`eurofs::badblocks::BadBlockTable`** — transparent remap of unrecoverable blocks (found by the G5 scrubber / I/O errors) to a spare pool (`translate`/`mark_bad`/`is_bad`, idempotent, pool-exhaustion → unrecoverable), serializable to survive reboot. **7 host tests.** **+ wired to real disk I/O** (`[j2]` boot self-test: LBA 48 marked bad → remapped to spare 50 → write/read transparently rerouted on the virtio-blk disk, verified). **+ MSI-X interrupt foundation** (`kernel/src/msix.rs`): PCI capability-list walk → MSI-X cap (0x11) → maps the table BAR → programs an entry with the **LAPIC message address/data** → enables MSI-X + disables INTx. **Boot-verified on the xHCI controller**: cap@0x90, table@`0xc000003000`, entry→`0xfee00000`/vec `0x46` unmasked, and a real USB transfer event delivers an MSI-X interrupt (`MSI-X-levering bevestigd ✓`, IDT vector 0x46 fired). The reusable delivery layer for interrupt-driven completion. **+ MSI-X on virtio-blk** (`[j2-blk]`): the storage controller delivers a message per request-completion (vector 0x47, queue_msix_vector=0); boot-verified — capacity still reads correctly (the legacy-virtio device-config shifts +4 when MSI-X on, so capacity is read *before* enabling it), `[g5]` scrub clean, +4 MSI-X completions for 4 disk reads. **Interrupt-driven completion is now real on both the USB input path (harvest in the IRQ) and the storage path.** REMAINING: make the completion *wait* block on the IRQ (needs a wait-queue) rather than the additive used-ring poll; NVMe MSI-X; live-FS auto-remap; multiple namespaces. |
| J3 | **Swap** | ✅ **DONE** | Core: **`euromm::swap`** — swap-slot allocator (`SwapArea`) + **CLOCK / second-chance** replacement (`Clock`). **5 host tests.** **+ real swap-cycle wired** (`[j3]`). **+ transparent fault-driven path** (`kernel/src/swapmgr.rs`): walks the active 4-level page tables, on swap-out marks the PTE **not-present with the swap-slot encoded in the upper bits**, frees the frame; the **page-fault handler** calls `try_swap_in` which reads the page back from disk, re-maps the PTE present and resumes the instruction — fully transparent. **Boot-verified** (`[j3-fault]`: page swapped out → access faults → handler swaps it in → 4 KiB pattern byte-intact; real/isolation faults still fall through correctly). REMAINING: wire CLOCK into the live frame-allocator under memory pressure (auto-evict). |

## Sprint K — Sovereign platform polish
| ID | Task | Kind | Notes |
|----|------|------|-------|
| K1 | **Full user & session model** | N 🧪 | Beyond `login`/`su`: multi-user home dirs, per-user EuroGuard policy, session lifecycle, `/etc/passwd`-style store with hashed creds. (Missing_Subsystems #13.) |
| K2 | **EuroUpdate delivery** | R 🧪 | Fetch signed update images over HTTPS (now that A1 validates certs) → feed G4's A/B apply; an update server + channel (stable/beta). |
| K3 | **Disk encryption (FDE)** | 🟢 **core done** | **`eurofde` crate** — length-preserving per-block **ChaCha20** FDE (nonce = volume-salt ‖ LBA, so equal plaintext blocks differ by position) + `EncryptedBlockDevice<D>` that impls `BlockDevice` → transparent FDE under any EuroFS. **3 host tests** (block roundtrip + position-dependence, disk holds ciphertext not plaintext, EuroFS mounts on the encrypted volume + wrong key fails). **Boot-verified** (`[k3]`: a real EuroFS formatted + read/written on the encrypted layer with the **256-bit key from the TPM** (O1), `sleutel-van-TPM=true`). REMAINING: seal the FDE key to the TPM PCR policy (needs O1 unseal); key enrolment in the installer (Q1); re-key/rotation. |
| K4 | **GPU accel (virtio-gpu)** | 🟢 | **Protocolkern gebouwd** (`eurogpu` crate, 6 host tests): virtio-gpu 2D-commando-serialisatie (GET_DISPLAY_INFO · RESOURCE_CREATE_2D · ATTACH_BACKING · SET_SCANOUT · TRANSFER_TO_HOST_2D · RESOURCE_FLUSH) + respons-parsing (displayinfo→resolutie, OK/ERR). Kernel `[k4]`-boot-zelftest + `gpu`-shell. **Boot-verified** ✓. REMAINING (hardware-attended): de **moderne virtio-transport** (virtio-gpu is virtio-1.0-only; de bestaande blk/net-drivers zijn legacy-poort-I/O) + framebuffer-scanout-binding + screenshot-verificatie. |

## Sprint L — Immutability subsystem 🔒
| ID | Task | Kind | Notes |
|----|------|------|-------|
| L1 | **EuroFS immutable flag** | ✅ **DONE** | Per-inode `IMMUTABLE` + `APPEND_ONLY` flags (`eurofs::FLAG_*`, on-disk inode offset 28); `write_file`/`remove_file`/`rename` reject mutation of immutable files and non-extending writes / deletes of append-only files — **even from ring-0**. `get_flags`/`set_flags` on the `FileSystem` trait, forwarded through the VFS. **3 host tests** (immutable blocks write/delete/rename, append-only only extends, flags survive remount). **Boot-verified** (`[l1]`: cap-gate + enforcement + clear, content intact). The boot-updater (`sync_system_files`) clears the flag before a legitimate `/bin` re-sync. |
| L2 | **`CAP_IMMUTABLE_ADMIN` capability** | ✅ **DONE** | New `CAP_IMMUTABLE_ADMIN` (ring3) — `kernel/src/immutable.rs::set_protected` gates *setting/clearing* immutability on it: `PermissionDenied` even for root without it. **Boot-verified** (`[l2]`: 6 real system files — `/bin/*`, `/lib/libeuro.so`, `/etc/shadow`, `/etc/hostname` — marked tamper-proof). REMAINING (L3/L4): verity Merkle-tree over a read-only slot partition (needs G4+K3); user-scoped immutability + `euroattr` shell cmds. |
| L3 | **Immutable system image partitions** | N 🔒 🏗️ | Read-only slot mount after the G4 loader; **verity-style Merkle block-hash tree** over the slot partition verified against the Ed25519 update manifest → tamper → rollback. Build-time flag pass sets `IMMUTABLE` on OS inodes. Depends on G4 + K3. |
| L4 | **User-accessible immutability API** | N 🧪 | User-scoped `IMMUTABLE` on own home files (no `CAP_IMMUTABLE_ADMIN`); `euroattr +i/-i/+a` shell commands; file-manager lock badge (post-H5). |

## Sprint M — Toolchain & developer experience
| ID | Task | Kind | Notes |
|----|------|------|-------|
| M1 | **EuroToolchain** | N 🏗️ | Native `x86_64-unknown-euroos` Rust std target + minimal `eurolibc` + gdb/lldb remote stub over serial/virtio-console. Prerequisite for self-hosting. |
| M2 | **Package manager (europkg)** | 🟢 | `europkg` crate (7 host tests): **dependency-resolver** — semver `Version` + `Constraint` (Exact/AtLeast/Caret), `Repo::resolve` → topologische install-volgorde (deps-eerst, hoogste passende versie), met detectie van NotFound/NoMatchingVersion/Conflict/Cycle. Kernel `[m2]`-boot-zelftest + `europkg`-shell. **Boot-verified** ✓. (`eupkg` doet al build/verify van getekende ZIP+manifest+SHA256+Ed25519-pakketten.) REMAINING: install/remove/upgrade-uitvoering, content-addressed store. |
| M3 | **Reproducible builds** | ✅ | `eurorepro` crate (6 host tests): deterministische `BuildSpec::id` (genormaliseerde/gesorteerde inputs → stabiele SHA-256), Ed25519-getekende `Attestation` (spec_id+output_hash), bit-voor-bit `reproduce`, **onafhankelijke-bouwers-`consensus`** (≥N unieke bouwers eens), volatiele-input-detectie (SOURCE_DATE_EPOCH e.d.). Kernel `[m3]`-boot-zelftest + `eurorepro`-shell. **Boot-verified** ✓. |

## Sprint N — Network completeness
| ID | Task | Kind | Notes |
|----|------|------|-------|
| N1 | **WiFi stack** | 🟢 | **Protocolkern gebouwd** (`eurowifi` crate, 6 host tests, dep sha2): 802.11-frame-parsing (mgmt/control/data + subtypes), **beacon→scan-result** (SSID/kanaal/security via RSN+capabilities, **WPA3-SAE-detectie** via AKM 00-0F-AC:8), **WPA2/3-PTK-afleiding** (IEEE-PRF op HMAC-SHA-256, richting-symmetrisch 48B). Kernel `[n1]`-boot-zelftest + `wifi`-shell. **Boot-verified** ✓. REMAINING (hardware-attended): de radio-driver zelf (AX200/210 PHY/MAC, iwlwifi-firmware-laden) + de volledige SAE-PAKE-handshake. |
| N2 | **Sovereign VPN tunnel** | 🟢 **core done** | **`eurovpn` crate** — a forward-secret, mutually-authenticated tunnel (Noise-style): static + ephemeral **X25519**, a **quadruple-DH** (`e·e`, `e·S`, `s·e`, `s·S`) → **HKDF-SHA256** session keys, **ChaCha20-Poly1305** transport with per-packet counter nonces. **3 host tests** (matching keys, wrong-peer rejected = mutual auth, tamper detected). **+ kernel `vpn.rs`** with TPM-seeded keys + a `vpn` shell command (shows the local public key, WireGuard-config-style). **Boot-verified** (`[n2]`: full handshake + encrypted round-trip). REMAINING: byte-compat WireGuard (BLAKE2s) for interop; UDP framing + `wg0` over the NIC; roaming/keepalive. |
| N3 | **Stateful packet filter** | 🟢 **core done** | **`eurofw` crate** — a 5-tuple rule engine (direction/proto/src+dst CIDR/ports → ACCEPT/DROP, first-match-wins, default policy) + counters. **4 host tests** (CIDR, default-deny allowlist, first-match, counters). **+ kernel `firewall.rs` wired into the RX path** (`net::service` drops filtered inbound packets, stealth — no RST/ICMP) + a `firewall` shell command. **Boot-verified** (`[n3]`: Telnet/NetBIOS + a blocklisted /24 dropped, HTTPS allowed). REMAINING: stateful conntrack (allow-established); egress rules; per-interface policy; hook EuroPol policy → rules. |

## Sprint O — Sovereign identity & trust
| ID | Task | Kind | Notes |
|----|------|------|-------|
| O1 | **TPM 2.0 driver** | ✅ **DONE** (core) | **`eurotpm` crate** — TPM 2.0 command encoding/parsing (`Startup`/`GetRandom`/`PCR_Read`/`PCR_Extend`, BE wire format, auth sessions). **5 host tests.** **+ kernel TIS-MMIO driver** (`kernel/src/tpm.rs`): locality request, burst-count FIFO command/response transport @ `0xFED40000`. **Boot-verified** on QEMU `tpm-tis` + swtpm (`[o1]`): TPM detected (DID_VID `0x00011014`), Startup, **GetRandom returns real entropy**, **PCR16 extend changes the PCR** (measured-boot mechanics). REMAINING: key sealing (`TPM2_CreatePrimary`/`Create`/`Load`/`Unseal`) + PCR-policy sessions → seal the K3 FDE key to boot PCRs; NV storage; CRB transport. |
| O2 | **Remote attestation** | ✅ | `euroattest` crate (6 host tests): nonce-gebonden **quote** over de echte measured-boot-PCR's (`tpm::read_pcr`), AK-getekend; verifier aanvaardt verse quote, weigert **replay** (nonce-mismatch) + **gewijzigde toestand** (PCR-mismatch). Kernel `[o2]`-boot-zelftest (echte TPM-PCR's + TPM-geseede AK) + `euroattest`-shell. **Boot-verified** ✓. REMAINING (attended): JSON-over-HTTPS challenge/response-endpoint over EuroNet; echte TPM-AK-quote-cmd ipv software-AK. |
| O3 | **EuroCA trust anchor** | ✅ | `euroca` crate (7 host tests): TPM-geseede **wortel-CA** (zelf-getekend), `issue` op CSR, `verify`-keten (handtekening + geldigheidsvenster + CA-vlag), **revocatie**, SHA-256-vingerafdruk. Eigen compact formaat (Ed25519), geen X.509/ASN.1. Kernel `[ca]`-boot-zelftest + `euroca`-shell. **Boot-verified** ✓. REMAINING: intermediates/sub-CA's, on-disk CA-store, koppelen aan code-signing/OTA-paden. |

## Sprint P — EU compliance & localisation
| ID | Task | Kind | Notes |
|----|------|------|-------|
| P1 | **EU locale support** | ✅ | `eurolocale` crate (28 host tests) — alle 24 officiële EU-talen: `lang` (codes/endoniemen/eurozone), `number` (groepering+decimaalteken), `currency` (€ of eigen munt BGN/CZK/DKK/HUF/PLN/RON/SEK + plaatsing), `datefmt` (Dmy/Ymd + maandnamen), `plural` (CLDR one/two/few/many/other per taal-systeem), `collation` (diacritiek-vouwing + taal-tailoring: sv `å/ä/ö` ná z, de `ä≈a`, es `ñ` ná n, cs `č` ná c). Kernel `[loc]`-boot-zelftest + `locale <tag>`-shell. **Boot-verified** ✓. REMAINING: maandnamen voor de overige 16 talen, keyboard-layouts (Q1). |
| P2 | **Accessibility layer** | 🟢 | `euroaccess` crate (6 host tests, dep eurolocale): **accessibility-boom** (`AccNode`: rol/naam/waarde/checked + kinderen, 11 ARIA-rollen) + **focusbeheer** (`focus_order` leesvolgorde, `move_focus` cyclisch vooruit/achteruit) + **meertalige schermlezer** (`announce` per taal: "knop: X"/"Schaltfläche: X"/"bouton: X", rol-labels uit EuroLocale; tekstveld+checkbox-toestand). Kernel `[p2]`-boot-zelftest + `euroaccess`-shell. **Boot-verified** ✓. REMAINING (attended): EuroDisplay vult de boom met echte widgets + live focus/text-events + audio-TTS-uitvoer. |
| P3 | **GDPR-native audit log** | 🟢 **core done** | `kernel/src/audit.rs`: in-memory event ring (`Event`: immutable set/cleared/denied, cap-denied, login/logout/boot + seq + tick) **persisted to `/var/log/audit.log` marked `APPEND_ONLY` (L1)** → tamper-evident (truncate/overwrite rejected by the FS, only extension allowed). `audit` shell command. **Boot-verified** (`[p3]`: 14 events, append-only flag set, tampering blocked, extending OK). REMAINING: log `execve`/connections too; JSON format + `euroaudit` query/export; rotation. GDPR 5(2)/NIS2. |

## Sprint Q — Distribution & governance
| ID | Task | Kind | Notes |
|----|------|------|-------|
| Q1 | **Installer / live image** | 🟢 | **Planner gebouwd** (`euroinstall` crate, 7 host tests, dep eurolocale): `validate` (schijf/gebruiker/locale/keymap) + `partition_layout` (ESP+EuroOS-A/B+EuroVar, 1MiB-uitgelijnd, niet-overlappend) + `plan`→geordende `Step`s (FDE vóór format · CreateUser · ProvisionEuroCa · FinalizeBoot) + aparte **live** RAM-only-modus (geen schijf-schrijfacties). Kernel `[install]`-boot-zelftest (13-staps plan ✓) + `euroinstall [live]`-dry-run-shell. **UITVOERING (✅):** `kernel/src/instexec.rs` voert het plan écht uit — `EuroFs::format` op een (RAM-)blockdevice + provisioneert /etc/locale.conf · /etc/keymap · /etc/hostname · /etc/passwd · /etc/euroca/root.crt; boot-`[q1x]`: **na een remount (≈ reboot) persisteren hostname/gebruiker/locale/EuroCA** ✓. Dezelfde executor draait op een virtio-blk-partitie voor een echte schijf-install. REMAINING (attended): de getekende userspace-installer-GUI + FDE-enrol-stap live. |
| Q2 | **Reproducible builds & release pipeline** | 🟢 | Kern gebouwd via **M3 `eurorepro`** (deterministische spec-id + getekende attestatie + onafhankelijke-bouwers-consensus, boot-verified). REMAINING (attended): CI-integratie + per-tag-pipeline (source-commit → binary-hash → Ed25519-manifest). |
| Q3 | **Open source governance** | N | EUPL-1.2 licence, DCO/CLA, coordinated CVE disclosure (90-day embargo), hardware-compatibility-list process. Release-blocking, non-code. |

---

## Sprints R–Z — Platform maturity (added 2026-06-05, from the "roadmap additions" doc)
*Filtered for feasibility in a Rust bare-metal kernel. Full module layouts + data structures + verify steps live in `docs/ROADMAP.md`.*
> **★ A prioritised, phased Fase-2 sprint plan (which of K–Z + EuroSuite to do, in what order, + own additions) lives in `docs/PHASE2-PLAN.md`.** Recommended first sprint: **R EuroDevice**.

| ID | Task | Kind | Notes |
|----|------|------|-------|
| R | **EuroDevice — driver framework & device model** | 🟢 **core done** | **`eurodevice` crate** built: `DeviceNode`/`DeviceTree` (BTreeMap, parent/child, reparent), `DriverRegistry` (register/bind/bind_all/unbind via match-predicates), `trait Driver{start/stop/suspend/resume}`, `HotplugQueue`. **6 host tests.** **+ kernel-wired** (`kernel/src/eurodevice.rs`): builds the tree from real PCI enumeration, registers the existing drivers (virtio-blk/net, nvme, xhci, hda, gpu, bridge) with their matchers, binds them. **Boot-verified** (`[r]`: 9 devices, 5 bound to the right drivers; correctly-unbound VGA/e1000/AHCI/legacy-USB) + **`eurodevice`/`lsdev` shell command** (shown working interactively via the E2E test). REMAINING: make binding *drive* the probe (so init runs via the framework, not separate calls); real hotplug IRQ wiring; suspend/resume across the tree. |
| S | **EuroSnap — snapshot & rollback** | ✅ **DONE** | CoW snapshots on EuroFS: a snapshot is a frozen root-pointer (`SnapshotEntry`: id/parent/timestamp/objmap_root/map_blocks/checkpoint/flags/label) in a reserved block (8); **`rebuild_allocator` pins each snapshot's blocks** so CoW never reclaims them. `snapshot_create/list/rollback/delete` on the `FileSystem` trait + VFS-forwarded; **GC** on delete. **4 host tests** incl. a large-file test proving block-pinning survives heavy post-snapshot allocation (rollback restores byte-for-byte). **Boot-verified on the live root FS** (`[s]`: snapshot → write test file → rollback drops it, system files intact, **scrub clean, 0 faults**). `eurosnap` shell command. REMAINING: writable clones; G4 auto-rollback wiring (snapshot before update → rollback on failed boot); `diff`. |
| S-fsck | **EuroFS fsck/repair** | ✅ **already existed** | `scrub()` (checksum + structure + data-path XXH3 verification) + `repair()` (heals a degraded A/B-superblock slot from the valid copy; auto-heals at mount via `heal_slots`). **Bitmap-free allocator** (free space reconstructed from the committed superblock each mount → can't desync). `fsck`/`fsck repair` shell commands. No new work needed. |
| T | **EuroContainer — native container runtime** | 🔒 ⭐⭐ | OCI-compatible containers on **EuroGuard capabilities** (not Linux namespaces) + EuroFS overlay layers + Ed25519-signed local registry. `ResourceLimits` (cpu_shares/mem_bytes/max_pids/io_weight). `euroctr run/pull/exec/list`. Needs H4 (WASM/WASI) for the WASI backend + S (overlay CoW). ~3–4 sessions. |
| U | **EuroVault — secrets & key management** | 🟢 **core done** | **`eurovault` crate** — capability-gated secrets store: `get(label, caps)` returns `EPERM` if `caller_caps` lacks the secret's `read_caps` (even root); `list` never leaks values; secret bytes **volatile-zeroed on drop**; `seal`/`unseal` the whole vault with **ChaCha20-Poly1305** (tamper-evident — wrong key / 1-bit flip → decrypt fails). **4 host tests.** **+ kernel `vault.rs`**: master key from the **TPM** (O1), every access → P3 audit; `vault`/`vault get` shell command. **Boot-verified** (`[u]`: master-van-TPM=true, cap-gated read, blob has no plaintext, wrong-key fails). REMAINING: persist the sealed blob + TPM-seal the master key to PCRs (O1 unseal); rotation/versioning; rate-limit. |
| V | **EuroIDM — enterprise identity** | 🟢 ⭐⭐ | `euroidm` crate (6 host tests): gebruikers/groepen + **groep→capability**-mapping (unie van maskers) + getekende **OIDC-achtige tokens** (Ed25519: subject/uid/groepen/vervaltijd) met verify (handtekening+expiry); privilege-escalatie (groep toevoegen ná tekenen) → BadSignature. Kernel `[v]`-boot-zelftest (TPM-geseede IDM) + `euroidm`-shell. **Boot-verified** ✓. REMAINING (attended): pluggable `IdentityBackend` LDAP-AD/OIDC-SAML over EuroNet-TLS + SSO-browserflow (H5) + offline-credential-cache. |
| W | **EuroObserve — native observability** | 🟢 **core done** | **`euroobserve` crate** — lock-free metric primitives (`Counter`/`Gauge`/`Histogram`, pure atomics → zero-overhead when unread) + a **OpenMetrics** (Prometheus text) renderer with cumulative histogram buckets. **4 host tests.** **+ kernel `observe.rs`**: live metrics (`euroos_syscalls_total`/`fs_reads`/`msix_irqs`/`free_frames`/`fs_read_us` histogram) + `metrics` shell command (renders OpenMetrics). **Boot-verified** (`[w]`: 13 metric values, real msix/free-frame counts, Prometheus-scrapable). REMAINING: a `/metrics` HTTP endpoint over EuroNet; W3C-TraceContext spans + OTLP; wire counters into the real syscall/FS hot paths. |
| X | **EuroPol — policy engine** | 🟢 **core done** | **`europol` crate** — declarative TOML-ish policy (`[allow]`/`[deny]` capabilities + paths, `deny` always wins) → an **EuroGuard capability mask** (`effective_caps(base) = (base\|allow) & !deny`) + path checks + human `explain`. **5 host tests.** **+ kernel `europol.rs`**: applies a firefox policy, `europol`/`europol explain` shell command, **violations → P3 audit**. **Boot-verified** (`[x]`: caps `0b11111→0b01111` — CAP_IMMUTABLE_ADMIN removed, CAP_NET kept; `/etc` denied; violation logged). REMAINING: Ed25519-signed policy bundles; per-binary policy attach; syscall-path cache; `simulate`. |
| Y | **EuroCrash — crash recovery framework** | 🟢 **core done** | **`eurocrash` crate** — a 512-byte minidump (`CrashDump`: vector/error/rip/rsp/rflags/cr2/cr3/16 GPRs/build-hash/uptime/seq, magic + checksum) encode/decode. **3 host tests.** **+ kernel `crashdump.rs`**: `capture()` hooked into the `#GP`/`#PF`(ring-0)/`#DF` handlers → writes a dump (with live cr2/cr3) to a reserved block (LBA 300) before halt; `read_last()` recovery-read on the next boot; `eurocrash` shell command. **Boot-verified** (`[y]`: minidump write→disk→read round-trip intact; recovery-read of the prior boot's dump). REMAINING: full dump (heap + PCBs); recovery-boot diagnostics shell via the G4 loader; backtrace symbolisation. |
| Z | **EuroHealth — system health engine** | 🟢 **core done** | **`eurohealth` crate** — parses the NVMe **SMART/Health log** (temperature, available-spare vs threshold, wear %, media errors, power-on hours, unsafe shutdowns) → `SmartStatus` + 0–100 score; combines disk + FS-scrub + memory into a `HealthReport` with an overall score + `Passed/Warning/Failed`. **4 host tests.** **+ kernel `health.rs`** (reads `nvme::smart_log()` + the live scrub + frame counts) + `eurohealth` shell command. **Boot-verified** (`[z]`: real NVMe SMART read → score 100/100 GEZOND, combined with FS + memory). REMAINING: background daemon + trend history; EuroDisplay/P3 alerts on score drop; ECC memory diagnostics; feed W metrics. |

*Deferred from the additions doc (out of current scope): A3 native hypervisor, A5 distributed storage, A10 native backup (revisit after S+N2), A11 hardware-compat DB (→ Q3 governance), A13 AI runtime, A14 sovereign-cloud integration.*
*Suggested order within R–Z:* **R → S → W → X → U → Y → Z → T → V** (R is the base for all new hardware; S strengthens G4 + T; W/X are small high-value adds; T/V depend on earlier sprints).

---

## EuroSuite — sovereign office suite (separate userspace product, added 2026-06-05)
*A from-scratch Rust office suite (Writer/Calc/Impress), NOT a LibreOffice port. Its own `euro-suite/` workspace; depends on the EuroOS userspace + EuroDisplay (H5) + EuroFS CoW + EuroGuard. Full plan in `docs/EUROSUITE-PLAN.md`. This is a large multi-month product track, parallel to the kernel roadmap.*

| ID | Task | Kind | Notes |
|----|------|------|-------|
| ES-Core | **Universal Document Model + shared engine** | 🟢 | `eurodoc` crate (6 host tests, GEEN deps): UDM `Document`(Writer/Sheet/Deck) + `Block`/`Paragraph`/`Run`/`RunProperties`/`Table`/`Cell`/`Slide` + `StyleRegistry` met overerving (cyclus-veilig) + tekstextractie + woord-/teken-/element-statistiek. **Boot-verified** (`[es]`) ✓. REMAINING (attended): layout+render-pijplijn (shaping/raster) in de compositor. |
| ES-IO | **OOXML + ODF + PDF I/O** | 🟢 | `eurodocio` crate (11 host tests, dep eurodoc): **eigen XML-pull-parser** (geen externe crate) + **OOXML** (.docx `document.xml`) lezen+schrijven (paragrafen/runs/`w:b`/`w:i`/`w:pStyle`, round-trip) + **ODF** (.odt `content.xml`) lezen (`text:h`/`text:p`/`text:span`) + **HTML-export**. `docx→UDM→HTML`-pijplijn boot-verified ✓. REMAINING (attended): ZIP-container (deflate) + .xlsx/.pptx/.ods/.odp + PDF-export. |
| ES-Calc | **EuroSuite Calc — formule-engine** | 🟢 | `eurocalc` crate (7 host tests, dep eurodoc): tokenizer + precedentie-parser + evaluator over `SheetBody` — celrefs `A1`, bereiken `A1:B3`, operatoren `+ - * / ^ %`, functies SUM/AVERAGE/MIN/MAX/COUNT/IF/ROUND/ABS, recursieve formule-cellen, **cyclusdetectie**, DivZero — geen libm. **Boot-verified** (`[es]`) ✓. REMAINING: meer functies (VLOOKUP/pivot), grid-UI, multi-sheet, charts. |
| ES-Writer | **EuroSuite Writer (.docx/.odt)** | 🟢 | Computationele kern AF op `eurodoc`+`eurodocio` (UDM + OOXML/ODF-lezen/schrijven + HTML-export + woordental). REMAINING (attended): de GUI-app (Slint-achtig: cursor/selectie/undo/redo/find&replace/pagination) in de compositor. |
| ES-Impress | **EuroSuite Impress (.pptx/.odp)** | 🟢 | Kern AF op `eurodoc` Deck (dia's + outline + tekstextractie). REMAINING (attended): de GUI-app (thumbnails, themes, shapes, fullscreen-presentatie, transitions). |
| ES-Int | **EuroOS integration** | 🔒 | EuroFS CoW version history in-UI (snapshots = S), EuroGuard document sandboxing (macros never auto-run, minimal caps by default), MIME registration with EuroDesktop, default-app settings, EuroNet CRDT collaboration (future). |

---

## EuroCoreutils — GNU-compatible userland (added 2026-06-05)
*Closes the coreutils gap: EuroOS has ~25 of the 108 GNU coreutils. A host-tested `userspace/eurocoreutils/` crate fills the rest, in 8 batches, each command a `shell.rs` match-arm + a host-tested `run(args, fs, stdout) -> i32`. Full plan + per-command list + GNU-compat test strategy in `docs/EUROCOREUTILS-PLAN.md`. Fits Sprint **M2** (europkg) as the userland layer; the `--cap`/`--sign`/`--immutable-check` extensions tie into **X**/L1. Reference (impl, not basis): uutils/coreutils (MIT).*

*Built so far in the **`eurocoreutils` crate** (18 host tests) + wired into `shell.rs` (a `coreutils()` dispatcher: the last existing file-arg is read as stdin, the rest are options). **Boot-verified** end-to-end (`seq 5` typed via the USB keyboard → `1 2 3 4 5`).*

| ID | Batch | Kind | Notes |
|----|-------|------|-------|
| CU-0 | **Shared infra** | ✅ | `args::Args` parser (short/combined/long flags, attached or separate values, positionals). |
| CU-1 | **Trivial** | ✅ | `echo · seq · basename · dirname · true · false · yes · arch · nproc · pwd`. (`sleep` → kernel; `hostname` already a built-in.) |
| CU-2 | **File ops** | 🟢 | `cp · touch · stat · truncate` on EuroFS (`stat` shows the L1 immutability flags). REMAINING: `ln · readlink · realpath · mktemp` (need FS symlinks); `cp --verify`/`--sign`. |
| CU-3 | **Text I/O** | ✅ | `head · tail · wc · tac · rev · fold · nl · cat -n`. (`paste` remaining.) |
| CU-4 | **Text transform** | 🟢 | `sort (-r/-n/-u) · uniq (-c/-d) · cut (-d/-f/-c) · tr (-d)`. REMAINING: `shuf · comm · join · split · csplit · fmt · od`. |
| CU-5 | **Search & filter** | 🟢 | `grep (-i/-v/-n/-c)` · `find` (eigen glob `*`/`?` + `-name`/`-type f\|d`/`-maxdepth`, VFS-boom-walk shell-zijde; boot-`[find]` zelftest ✓). REMAINING: `xargs`; ★ `grep --audit`→P3. |
| CU-6 | **Checksums & encoding** | 🟢 | `sha256/512/224/384sum · base64 (-d) · base32 (-d) · cksum` (all host-tested vs known vectors). REMAINING: `sha1sum · md5sum · b2sum` (need crates); ★ `--sign`. |
| CU-7 | **Env & control** | 🧪 | `env · printenv · printf · expr · tee · test`/`[` · numfmt · factor`. |
| CU-8 | **Print & misc (low pri)** | 🧪 | `pr · tsort · ptx`. |

*Each command is a pure `fn(args, input) -> Vec<u8>` host-tested against expected GNU output. ✅ **Pipe-stdin done**: `shell::run_pipeline` threads stdout→stdin between built-ins (`cat f | grep x | sort`), `tee FILE` writes+passes through, `head/tail -N` GNU-shorthand; the desktop terminal delegates all-builtin pipelines to the pipe-aware shell. Boot-`[pipe]` verified. ⬜ long-tail utils remain (ln/readlink, shuf/comm/join/split, sha1/md5/b2sum, env, xargs).*

---

## Sprint AA — EuroAgent: sovereign agent-first runtime (added 2026-06-05) ⭐ strategic
*The sovereign answer to Microsoft's Project Solara (Build 2026, agent-first Android+Entra). EuroOS's differentiator: **AI agents run capability-isolated at the kernel level, fully audited, with no mandatory external identity/cloud** — the trust boundary is in the kernel (EuroGuard), not a US cloud. Agents = WASM modules + a declarative capability manifest; an MCP gateway exposes EuroOS subsystems as tools; a local LLM is the default (cloud opt-in via EuroVault). Full plan in `docs/EUROAGENT-PLAN.md`. Deps: **H4** (WASM/WASI) · **L1** (immutable) · **P3** (audit) · **U** (vault) · **X** (policy) · **O1** (TPM) — most now built. ~4–5 sessions.*

*Built so far in the **`euroagent` crate** (40 host tests) + kernel module `kernel/src/agent.rs`, **boot-verified** (`[aa]` self-test: manifest→least-privilege caps→cap-gated MCP call→intent-route→**full LLM↔MCP agent loop**, all green). Shell command `euroagent`/`agent`.*

| ID | Step | Kind | Notes |
|----|------|------|-------|
| AA-1 | **AgentManifest + bundle + registry** | ✅ | `manifest.rs`: doelgerichte TOML-subset-parser → `AgentManifest`; verwerpt onbekende caps + ontbrekende velden. `bundle.rs`: **Ed25519-`.euroa`-bundle-verify** (domein-gescheiden, lengte-geprefixte handtekening over `manifest‖wasm`; vervalst manifest/WASM → geweigerd). `registry.rs`: **`AgentRegistry`** — installeren enkel via geldige bundle, naam vastgezet aan de uitgever-pubkey (**anti-kaping**: andere uitgever kan een naam niet overschrijven, ook met eigen geldige handtekening), serialiseerbare index voor EuroFS-persistentie. `euroagent list`-shell. Boot-`[aa]`: geldige bundle OK + vervalste geweigerd + install + hijack-geblokkeerd ✓. |
| AA-2 | **AgentCaps + EuroGuard** | ✅ | `caps.rs` (16 fijnmazige `AgentCaps`-bits) + `policy.rs`: `effective = (required\|granted_optional) ∩ user_caps − EuroPol_denied`, met `CapDecision` (dropped_by_user/dropped_by_policy/needs_confirmation). **Nooit meer dan de parent-user.** Boot bewijst NET-drop(beleid) + EXEC-drop(user-clamp). |
| AA-3 | **MCP gateway** | 🟢 | `mcp.rs` + `json.rs` (eigen no_std JSON): JSON-RPC 2.0 `tools/list`+`tools/call`, 10 tools elk gated op hun `CAP_AGENT_*`, elke call → `AuditRecord` (P3-vorm). **ÉCHTE backend (Phase 2C):** `agent.rs::FsToolBackend` koppelt `fs_write`/`fs_read`/`display_notify` aan EuroFS, elke agent geklemd in zijn sandbox-map `/agents/<naam>/` (pad-`..`-strip). Boot-`[aa-fs]`: agent schrijft+leest écht op schijf, escape geblokkeerd, zonder-cap geweigerd ✓. **AF_UNIX-DAEMON (✅):** `kernel/src/mcpd.rs` serveert de gateway over een echte AF_UNIX-socket (`/run/euroagent/mcp.sock`, H1-switchboard); boot-`[aa-mcp]`: client verbindt→JSON-RPC tool-call over socket→daemon verwerkt via cap-gated gateway op EuroFS→antwoord terug→bestand écht geschreven ✓. REMAINING: ring-3-daemon (nu in-kernel gehost zoals H2) + net_get/vault_get-backends. |
| AA-4 | **EuroDispatch orchestrator** | 🟢 | `intent.rs`: deterministische intent→agent routing (taal-onafhankelijke woord-overlap-score, geen AI). REMAINING: userspace-daemon, agent-spawn via kernel-manager, workflows, user-confirm voor elevated caps (`needs_confirmation` is er al). |
| AA-5 | **Agent loop + local LLM** | 🟢 | `llm.rs`: `LlmBackend`-trait + `Message`/`LlmResponse` (Text\|ToolCall) + **Ollama-`/api/chat`**-request/response-(de)serialisatie (lokale default, soeverein). `agentloop.rs`: deterministische lus `model→tool→MCP-gate→resultaat→model→eindantwoord`, met audit + cap-gate + step-limiet. Boot-`[aa]`: gescript model doet fs_write(toegestaan)+exec(geweigerd door cap-gate)→eindantwoord ✓. **Echte Ollama-HTTP (Phase 2C):** `ollama_http_request` (volledige HTTP/1.1 POST `/api/chat` + Content-Length) + `parse_http_response` (host-getest); `euroagent llm`-shell. **WASM-AGENT-HOST (✅):** `kernel/src/wagent.rs` draait een echte WASM-agentmodule (EuroWASM-interpreter) waarvan de host-import `agent.fs_write` door de cap-gated MCP-gateway → EuroFS loopt; boot-`[aa-wasm]`: mét FS_WRITE schrijft de WASM-agent écht een bestand, zónder cap weigert de gateway (status 0) ✓. **Volledige keten: WASM-code→host-import→capability-poort→MCP-gateway→audit→EuroFS, trust boundary in de kernel.** REMAINING (attended): echte Ollama-TCP-transport over EuroNet, opt-in cloud, `euroagent install/run/grant`. |
| AA-ref | **Reference agents** | 🧪 | Ship 4: `priority` (always-on dashboard), `facilitator` (meeting record/transcribe via Whisper.cpp-WASM), `incident-reporter` (NIS2/CyFun), `sys-monitor` (ties into Z EuroHealth). |

*Sovereignty test (the headline): NIC fully removed → agents stay fully functional on the local LLM; `euroagent audit` shows **0 cloud calls** in a whole session. Done: AA-1✅ AA-2✅ AA-3🟢 AA-4🟢 AA-5🟢 (core host-tested + boot-verified — manifest, caps/policy, MCP gateway, intent routing, and the full LLM↔MCP agent loop). Remaining: AA-ref + the userspace MCP daemon (AF_UNIX socket + real EuroFS/EuroNet/EuroVault backends + real Ollama HTTP) + WASM agent-code host + Ed25519 `.euroa` bundle verify.*

---

## Recommended order (value × safety) — updated
1. **Finish G1 + G4** — close the two started cores (page-fault IST + scheduler/IST stacks; multi-partition GPT + loader split). Highest robustness-per-line; production-critical.
2. **H3 (ld.so)** — dynamic linker; pair with a busybox/curl/sqlite breadth pass.
3. **L1 + L2** — EuroFS immutable flag + `CAP_IMMUTABLE_ADMIN`. Small surface, high security value; unlocks L3/L4.
4. **J1 (per-subsystem locking)** — convert working SMP into *scalable* SMP.
5. **I1 (USB) + N1 (WiFi)** — the gate to real-hardware daily use (WiFi uses the USB path).
6. **O1 (TPM 2.0)** — hardware root of trust; pairs with K3 (FDE) for sealed keys.
7. **H4 (WASM/WASI)** then **H5 (X/Wayland + Flatpak)** — sovereign + compat app ecosystems.
8. **K2 (signed OTA) + K3 (FDE) + K1 (users)** — complete the sovereign-platform promise; L3 (verity) follows K3.
9. **L3 + L4** — immutable system partitions + user-facing immutability API.
10. **N2 (WireGuard) + N3 (packet filter) + O2 (attestation)** — network sovereignty + zero-trust.
11. **M1 (EuroToolchain) + M2 (europkg)** — self-hosting + distribution layer.
12. **P1–P3** — EU localisation, accessibility, GDPR audit log.
13. **Q1 (installer) + Q2 (repro builds) + Q3 (governance)** — public-launch readiness.
14. **R (EuroDevice driver framework)** — *do early in the next phase*: it's the base that the now-built xHCI/HDA drivers (and all future ones) should migrate onto; unblocks clean hotplug.
15. **S (EuroSnap) + W (EuroObserve) + X (EuroPol)** — high value-per-line; S strengthens G4, W/X build on the lock-free kmsg + capability layers already shipped.
16. **U (EuroVault) + Y (EuroCrash) + Z (EuroHealth)** — secrets/crash-dumps/health; depend on O1/L1/NVMe-SMART.
17. **T (EuroContainer) + V (EuroIDM)** — larger, depend on H4/H5 + earlier sprints.
18. **EuroSuite** — separate multi-month userspace product track (its own `euro-suite/` workspace); start ES-Core + ES-IO once H5 display is solid.

> Full rationale, *why/how/verify* per item, and dependency notes live in **`docs/ROADMAP.md`**; the office suite in **`docs/EUROSUITE-PLAN.md`**.

## Snapshot of what's *done* (so it's not re-planned)
A1✅ A2✅(→G1) A3✅ · B1✅ B2✅ B3✅ · C1✅ C2✅ C3✅ · E1✅ E2✅(→H2) F1✅ F2✅ · **Sprint G ✅ (G1–G5)** · **Sprint H: H1✅ H2✅ H3✅ H4✅ H5✅** · **Sprint I: I1✅ (xHCI HID + USB mass storage) I2✅ (Intel HDA audio) I3✅ (AML interpreter on real DSDT) I4✅ (IPP + HTTP transport)** · **Sprint J: J1🟢 (lock-free kmsg + concurrent live-FS RwLock cache; per-CPU run-queue already exists) J2🟢 (MSI-X foundation verified) J3✅ (transparent fault-driven swap)**. Observability(now lock-free), ACPI+AML, PCI+MSI-X, scheduler+SMP+per-CPU-queues, EuroFS+VFS+NVMe+A/B-GPT+scrubber+concurrent-cache, TLS 1.3, EuroGuard, IPC, AF_UNIX, display server, guarded stacks, **xHCI USB (HID+storage), HDA audio, transparent swap** — all built & boot-verified. **255 host tests green; 15 lib crates.**
> **Remaining within I/J** (hard edges): I3 `_TMP`/`_BST` (QEMU exposes no thermal/battery) + S3 suspend (not headless-verifiable); I4 live TCP send to a CUPS server; J2 virtio-blk completion-via-IRQ; J1 fine-grained per-mount/per-connection locks. **Next phase = Sprints R–Z + EuroSuite** (above).

---

## Sprint BB — Attended bring-up: the last mile (added 2026-06-06) 🔌 hardware/network/GUI

*Everything with a host-testable/algorithmic core is now built and boot-verified (484 host tests, 41 crates, 19 boot self-tests). What remains is the **attended tier**: work whose **full** verification needs real/emulated hardware, a live network peer, or pixel/screenshot inspection — it cannot be proven by a deterministic serial-log self-test alone. Each item below already has its protocol/logic core done; BB closes the last mile. Kind: 🔌 attended (needs devices/network/GUI), ⭐ where it unlocks a headline.*

| ID | Step | What's already done (core) | What BB adds (the last mile) | Verify |
|----|------|----------------------------|------------------------------|--------|
| BB-1 | **Real Ollama TCP transport** ⭐ | `euroagent::llm` builds the HTTP/1.1 `/api/chat` request + parses the response (host-tested); `[aa]` agent-loop proven on a mock LLM | Open a TCP connection over **EuroNet** to `localhost:11434` (or a configured host), send the request bytes, read the chunked/`Connection: close` body, feed it into the agent loop. Cloud opt-in: key from **EuroVault**, every cloud call → P3 audit. `euroagent llm test`. | A reachable Ollama (or any HTTP echo) endpoint; assert a real model turn drives a tool call. |
| BB-2 | **Modern-virtio + virtio-gpu driver** ⭐ | `eurogpu` 2D command protocol + response parse (host-tested), `[k4]` deterministic | A **modern-virtio (virtio-1.0) transport** layer (PCI capability discovery: common/notify/ISR/device cfg over MMIO BARs — the existing blk/net are legacy port-I/O). Then bind virtio-gpu: control-vq, `GET_DISPLAY_INFO`, create-2d framebuffer, attach backing, set-scanout, transfer+flush → present the EuroDesktop on the virtio-gpu scanout. | QEMU `-device virtio-gpu-pci` + a **screenshot** showing the desktop on the virtio-gpu plane. |
| BB-3 | **WiFi radio driver** | `eurowifi` 802.11 frame parse + beacon→scan + WPA2/3 PTK (host-tested), `[n1]` | The **AX200/AX210 driver**: iwlwifi-style firmware load over the PCIe MMIO, MAC/PHY init, TX/RX rings, scan → connect → 4-way handshake using `eurowifi`'s PTK. (Full WPA3-SAE PAKE handshake also lands here.) | Real Intel WiFi hardware **or** a `mac80211_hwsim`-style emulation; associate + DHCP. |
| BB-4 | **Printer driver** | `europrint` IPP request/response (RFC 8010/8011) build+parse (host-tested) | USB printer-class (xHCI bulk) **or** live IPP-over-TCP to a network printer/CUPS; render a UDM (EuroDoc) page → PostScript/PCL or PDF and submit a print job. | A USB printer / a reachable CUPS server; print a test page. |
| BB-5 | **EuroSuite GUI apps** ⭐ ✅ | `eurodoc` UDM + `eurodocio` (OOXML/ODF/HTML) + `eurocalc` formula engine (all host-tested), `[es]` | The three apps **rendered in the compositor** (EuroDisplay/H2+H5): Writer (cursor + selection + text layout + pagination), Calc (grid + cell editing + live formula recalc), Impress (slide canvas + outline + present mode). Font shaping + raster for the document layout pipeline. | Screenshot of each app showing a real document/sheet/deck. |
| BB-6 | **EuroAgent dispatch GUI** | runtime + MCP daemon + WASM host + intent router all built & boot-verified | A desktop **agent panel**: intent input box → `intent::route` → run the agent loop, show the audited tool calls live, capability-grant prompts for elevated caps (`needs_confirmation` already computed). | Screenshot: type an intent, watch an agent act under the cap-gate. |
| BB-7 | **Installer GUI + live FDE-enrol** | `euroinstall` planner + `instexec` real format/provision (boot-verified, survives remount) | A guided **graphical installer** (partition picker, locale/keyboard via EuroLocale, user, **live FDE enrol** sealing the key to the TPM) running as a signed userspace process; + the RAM-only live-boot mode. | Run the installer in QEMU against a scratch disk → reboot into the installed system. |
| BB-8 | **Accessibility live-events + TTS** | `euroaccess` tree + focus + multilingual screen-reader announcements (host-tested), `[p2]` | Wire EuroDisplay widgets → the live `AccNode` tree; emit focus/text-change events; an **audio TTS** backend speaks the announcements through EuroAudio (HDA). EN 301 549 end-to-end. | Screenshot/audio: tab through a real dialog, hear/inspect the announcements. |

**Order:** BB-1 (cheapest, unlocks real agents) → BB-2 (modern-virtio is a prerequisite for much future HW) → BB-5/BB-6/BB-7 (GUI, share the compositor work) → BB-3/BB-4/BB-8 (device-specific). Each lands with the same discipline: extend the host-tested core where possible, then an attended verification (screenshot / live peer / real device), then docs + memory.

*Tell me a sprint or ID (e.g. "finish G4", "do H3", "do L1", "Sprint N WiFi", "BB-1 Ollama", "BB-2 virtio-gpu") and I'll run it with host tests + boot-verify (or attended verify) + docs.*

---

## Sprint AB — EuroBrowser (added 2026-06-06) 🌐 two-track
*Full plan: [docs/EUROBROWSER-PLAN.md](docs/EUROBROWSER-PLAN.md). A browser for EuroOS: Firefox bridge now, own engine as the endpoint.*

| ID | Step | What | Verify |
|----|------|------|--------|
| AB-A1 | Firefox dependency audit | Run the FF binary on the musl bridge, log failing `dlopen`/symbols → minimal stub list | List of missing symbols |
| AB-A2..A6 | Firefox compat stubs | `libs/compat/` shims: libX11/libEGL/libGL→`eurodisplay`, libdbus→NULL, libpulse→`euroaudio`; EuroGuard sandbox (CAP_NET/DISPLAY/FILE_READ, no EXEC/VAULT) | Firefox window in `eurodisplay`, euro-os.eu loads, sandbox holds |
| AB-B1 | **EuroWeb HTML5 tokenizer + DOM** ⭐ ✅ | `crates/euroweb` `html/`: spec state-machine tokenizer + tree construction → DOM. no_std+alloc, host-tested | ≥90% HTML5lib tokenizer cases (host tests) |
| AB-B2 | EuroWeb CSS engine ✅ | tokenizer + parser + selector matching (specificity/cascade) + computed style | ≥95% css-selectors suite (host tests) |
| AB-B3 | EuroWeb layout (block boxmodel) ✅ + paint | block + inline (line-breaking) layout → display list → raster → `eurodisplay`; reuse AA font rasterizer | `euroweb https://euro-os.eu/docs/` renders correctly |
| AB-B4 | EuroWeb flexbox ✅ (forms+images next) | Flexbox, `<form>` controls, navigation/history, cookies, `<img>` PNG/JPEG/WebP/SVG | Static interactive sites work |
| AB-B5 | EuroWeb JavaScript ✅ (eurojs) | tree-walking interpreter (no JIT); per-tab `JsInterpreter` with restricted caps; `fetch()` needs CAP_NET | wikipedia.org navigable; `fetch()` w/o CAP_NET → SecurityError |
| AB-B6 | EuroWeb LIVE fetch+render ✅ (echte HTTP-pagina) | tabbar/toolbar/address bar + security badge (EuroTLS/EuroCA); privacy-on-by-default; EuroVault passwords; zero telemetry | Screenshot of the browser shell |

**Order:** AB-B1 (this sprint) → B2 → B3 (static web milestone) → A1..A6 in parallel (Firefox bridge for users now) → B4 → B5 → B6. Transition: EuroWeb B2 → Firefox deprecated; B3 → Firefox removed from default install.

## Sprint AC — EuroApps: the missing desktop apps (added 2026-06-06) 🖥️ everyday usability
*Full plan: [docs/EUROAPPS-PLAN.md](docs/EUROAPPS-PLAN.md). 20 apps, each = host-tested core crate + `suite_ui`-style renderer + boot self-test, with a sovereign hook.*

| Fase | Apps | Lead sovereign hooks |
|------|------|----------------------|
| **AC-1** day-1 (low cx, reuse cores) | EuroReken ✅ · EuroNotes ✅ · EuroSafe ✅ · EuroMedia ✅ · EuroFiles ✅ · EuroShot ✅ | signed screenshots · reuse eurocalc · ⭐ realtime capability dashboard · append-only notes · privacy EXIF · immutability badges |
| **AC-2** first week | EuroClip ✅ · EuroClock ✅ · EuroFont ✅ · EuroView · EuroArchive ✅ | sovereign PDF · GDPR clipboard · EuroLocale times · signed fonts · signed archives |
| **AC-3** serious use | EuroContacts ✅ · EuroCalendar ✅ · EuroMusic ✅ · EuroCapture | EuroAgent backend · CardDAV sovereign · no telemetry · native framebuffer |
| **AC-4** full platform | EuroMail ✅ · EuroVideo · EuroVPN GUI · EuroSign ✅ · EuroHealth GUI | EuroCA S/MIME · soft-decode · built-in VPN · ⭐ sovereign signing · disk+FS+security health |

**Order:** lead with **EuroShot → EuroReken → EuroSafe** (highest value × lowest new infra — they reuse the compositor framebuffer, the eurocalc engine, and existing EuroGuard/audit data respectively). Then down the Fase list.

*Tell me an ID (e.g. "AB-B1", "build EuroShot", "EuroSafe dashboard") and I'll run it with host tests + boot/attended verify + docs.*

## Sprints AD–AG — "Promise = Reality": Zero Trust for AI agents + sovereign identity (added 2026-06-10) ⭐ CURRENT
*Full plan: [docs/SPRINT-PLAN-AD-AG.md](docs/SPRINT-PLAN-AD-AG.md) — derived from the prioritized backlog in [docs/AGENT-BRIEFING.md](docs/AGENT-BRIEFING.md) §7. Goal: close the gap between what /platform/ + /zero-trust/ publicly promise and what the code proves.*

| Sprint | Doel | Taken | Sessies |
|--------|------|-------|---------|
| **AD** `N 🔒` EuroAgent echt | echte tools + echt model + persistente audit | AD-1 `net_get`/`vault_get` MCP-backends (cap + domain-allow-list; `exec` blijft denied) · AD-2 NetOllama default, `[mock]`-label · AD-3 hash-chain audit append-only op EuroFS, cross-boot verify | 3–4 |
| **AE** `R 🔒` EuroID end-to-end | identiteit die onthoudt + echt inlogt | AE-1 store persist `/etc/euro/*.db` (immutable flags) · AE-2 login/su/passwd/desktop → `euroid::authenticate` (Argon2id) | 2–3 |
| **AF** `U 🔒` ZT-gaten dichten | geadverteerde 🟡's → ✅ | AF-1 PCR-seal vault+FDE-sleutels · AF-2 JIT-cap-elevatie + auto-revoke · AF-3 gedrags-baseline + audited alerts | 3–4 |
| **AG** `U` breedte (optioneel/parallel) | zichtbare gebruikerswaarde | AG-1 EuroApps-GUI's · AG-2 browser img/forms · AG-3 installer-executie · AG-4 coreutils-long-tail · AG-5 TTS-verkenning | 1–2 p/item |

**Order:** AD → AE → AF (AG parallel waar het past). Website-claims pas bijwerken nádat de code groen is.
