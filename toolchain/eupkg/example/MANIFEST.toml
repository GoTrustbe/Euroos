[package]
name = "hello"
version = "0.1.0"
description = "First EuroOS program — compiled with the EuroToolchain"
license = "EUPL-1.2"
binary = "bin/hello"

[build]
reproducible = true

[sandbox]
network = false
filesystem = "none"
